# Security

Before making this repository public:

- redact personal email addresses;
- redact AWS account IDs if your organization requires it;
- never commit AWS access keys, secret keys, or session tokens;
- never commit Okta API tokens;
- never commit passwords or private keys;
- do not commit a sensitive kubeconfig.
