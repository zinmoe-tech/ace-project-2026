# Evidence Images

Put the redacted evidence screenshots in this directory using the filenames listed in `docs/EVIDENCE.md`.

The Markdown documentation already references those filenames, so the photos will display automatically in GitLab after they are added.
