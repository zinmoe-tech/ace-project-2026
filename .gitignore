.terraform/
terraform.tfstate
terraform.tfstate.*
.terraform.tfstate.lock.info
terraform.tfvars
*.auto.tfvars
crash.log
crash.*.log

.DS_Store
*.pem
*.key
id_rsa
id_rsa.pub

*.log

# local-only secret, never commit
k8s-idp/reset password

# Vault root token + unseal keys — NEVER commit
vault-init.json
**/vault-init.json
