# Allows the global HTTPRoute (in global-api-gateway-ns) to reference
# the grc IngressGateway Service (now in grc-ingress).
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-global-httproute
  namespace: grc-ingress
spec:
  from:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    namespace: global-api-gateway-ns
  to:
  - group: ""
    kind: Service
    name: grc-istio-ingressgateway
